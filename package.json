{
  "name": "funnel-editor",
  "version": "1.0.0",
  "description": "Marketing Funnel Editor with React + TypeScript",
  "private": true,
  "homepage": ".",
 "scripts": {
  "start": "node server.js",         // Cloud Run 后端启动
  "build": "react-scripts build",   // 构建 React 静态文件
  "predeploy": "npm run build",     // 保留 gh-pages 部署前端
  "deploy": "gh-pages -d build"     // 保留 gh-pages 部署前端
}
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.5",
    "react-router-dom": "^6.22.0",  
    "firebase": "^10.12.3",        
    "ajv": "6.12.6",
    "ajv-keywords": "3.5.2",
    "zxcvbn": "^4.4.2"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0"
    
  }
}
 
